#!/bin/bash
mode: 'inference'
use_cuda: 1
num_gpu: 1
sampling_rate: 16000
network: "MossFormerGAN_SE_16K"

one_time_decode_length: 10
decode_window: 1

win_type: 'hamming'
win_len: 400
win_inc: 100
fft_len: 400
