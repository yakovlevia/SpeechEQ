{%- set display_name = (package or root or module | default('')).split('.')[-1] if (package or root or module) else 'Modules' %}
{{ display_name }}
{{ '=' * display_name | length }}

{%- if package or module %}
.. currentmodule:: {{ package | default(module) }}

.. automodule:: {{ package | default(module) }}
   :members:
   :undoc-members:
   :show-inheritance:
{%- endif %}

{%- if subpackages %}

Subpackages
-----------

.. toctree::
   :maxdepth: 5
   :caption: {{ display_name }}

{%- for sub in subpackages %}
   {{ sub.split('.')[-1] }} <{{ sub }}>
{%- endfor %}
{%- endif %}

{%- if submodules %}

Submodules
----------

.. toctree::
   :maxdepth: 5
   :caption: {{ display_name }}

{%- for mod in submodules %}
   {{ mod.split('.')[-1] }} <{{ mod }}>
{%- endfor %}
{%- endif %}