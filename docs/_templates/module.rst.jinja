{%- set display_name = (module or package or root | default('')).split('.')[-1] if (module or package or root) else 'Module' %}
{{ display_name }}
{{ '=' * display_name | length }}

.. currentmodule:: {{ module | default(package) }}

.. automodule:: {{ module | default(package) }}
   :members:
   :undoc-members:
   :show-inheritance: